# aes_cipher.py
# AES-128 implementation, no third-party libraries

S_BOX = [
    ,
]

INV_S_BOX = [
    ,
]

RCON = [
    0x00, 0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80, 0x1b, 0x36,
]


def gmul(a, b):
    """Nhân trong GF(2^8)"""
    p = 0
    for _ in range(8):
        if b & 1:
            p ^= a
        hi = a & 0x80
        a = (a << 1) & 0xFF
        if hi:
            a ^= 0x1b
        b >>= 1
    return p


def sub_word(word):
    return [S_BOX[b] for b in word]


def rot_word(word):
    return word[1:] + word[:1]


def key_expansion(key: bytes):
    """Sinh 11 round key cho AES-128 (mỗi round 16 byte)"""
    assert len(key) == 16
    Nk = 4
    Nb = 4
    Nr = 10
    w = [[0] * 4 for _ in range(Nb * (Nr + 1))]  # 44 word

    # 4 word đầu từ key gốc
    for i in range(Nk):
        w[i] = [key[4 * i + j] for j in range(4)]

    for i in range(Nk, Nb * (Nr + 1)):
        temp = w[i - 1].copy()
        if i % Nk == 0:
            temp = sub_word(rot_word(temp))
            temp[0] ^= RCON[i // Nk]
        w[i] = [(w[i - Nk][j] ^ temp[j]) & 0xFF for j in range(4)]
    return w


def get_round_key(expanded_keys, round_index):
    start = round_index * 4
    rk_words = expanded_keys[start:start + 4]
    rk = []
    for word in rk_words:
        rk.extend(word)
    return rk


def add_round_key(state, round_key):
    for i in range(16):
        state[i] ^= round_key[i]


def sub_bytes(state):
    for i in range(16):
        state[i] = S_BOX[state[i]]


def inv_sub_bytes(state):
    for i in range(16):
        state[i] = INV_S_BOX[state[i]]


def shift_rows(state):
    # state index = row + 4*col (ma trận 4x4 cột-major)
    rows = [state[r::4] for r in range(4)]
    for r in range(1, 4):
        rows[r] = rows[r][r:] + rows[r][:r]
    for r in range(4):
        state[r::4] = rows[r]


def inv_shift_rows(state):
    rows = [state[r::4] for r in range(4)]
    for r in range(1, 4):
        rows[r] = rows[r][-r:] + rows[r][:-r]
    for r in range(4):
        state[r::4] = rows[r]


def mix_single_column(col):
    a0, a1, a2, a3 = col
    r0 = gmul(a0, 2) ^ gmul(a1, 3) ^ a2 ^ a3
    r1 = a0 ^ gmul(a1, 2) ^ gmul(a2, 3) ^ a3
    r2 = a0 ^ a1 ^ gmul(a2, 2) ^ gmul(a3, 3)
    r3 = gmul(a0, 3) ^ a1 ^ a2 ^ gmul(a3, 2)
    return [r0 & 0xFF, r1 & 0xFF, r2 & 0xFF, r3 & 0xFF]


def mix_columns(state):
    for c in range(4):
        col = [state[4 * c + r] for r in range(4)]
        col = mix_single_column(col)
        for r in range(4):
            state[4 * c + r] = col[r]


def inv_mix_single_column(col):
    a0, a1, a2, a3 = col
    r0 = gmul(a0, 0x0e) ^ gmul(a1, 0x0b) ^ gmul(a2, 0x0d) ^ gmul(a3, 0x09)
    r1 = gmul(a0, 0x09) ^ gmul(a1, 0x0e) ^ gmul(a2, 0x0b) ^ gmul(a3, 0x0d)
    r2 = gmul(a0, 0x0d) ^ gmul(a1, 0x09) ^ gmul(a2, 0x0e) ^ gmul(a3, 0x0b)
    r3 = gmul(a0, 0x0b) ^ gmul(a1, 0x0d) ^ gmul(a2, 0x09) ^ gmul(a3, 0x0e)
    return [r0 & 0xFF, r1 & 0xFF, r2 & 0xFF, r3 & 0xFF]


def inv_mix_columns(state):
    for c in range(4):
        col = [state[4 * c + r] for r in range(4)]
        col = inv_mix_single_column(col)
        for r in range(4):
            state[4 * c + r] = col[r]


class AESCipher:
    """
    AES-128 ECB với padding PKCS#7.
    Dùng cho việc mã hoá / giải mã file nhị phân.
    """

    def __init__(self, key: bytes | str):
        if isinstance(key, str):
            key = key.encode("utf-8")
        # đảm bảo đúng 16 byte
        if len(key) < 16:
            key = key.ljust(16, b"\0")
        elif len(key) > 16:
            key = key[:16]
        self.key = key
        self.round_keys = key_expansion(self.key)

    @staticmethod
    def pad(data: bytes) -> bytes:
        pad_len = 16 - (len(data) % 16)
        return data + bytes([pad_len]) * pad_len

    @staticmethod
    def unpad(data: bytes) -> bytes:
        if not data:
            return data
        pad_len = data[-1]
        if pad_len < 1 or pad_len > 16:
            raise ValueError("Invalid padding")
        return data[:-pad_len]

    def encrypt_block(self, block: bytes) -> bytes:
        assert len(block) == 16
        state = list(block)

        add_round_key(state, get_round_key(self.round_keys, 0))
        for rnd in range(1, 10):  # 9 round đầu
            sub_bytes(state)
            shift_rows(state)
            mix_columns(state)
            add_round_key(state, get_round_key(self.round_keys, rnd))

        # round cuối không MixColumns
        sub_bytes(state)
        shift_rows(state)
        add_round_key(state, get_round_key(self.round_keys, 10))

        return bytes(state)

    def decrypt_block(self, block: bytes) -> bytes:
        assert len(block) == 16
        state = list(block)

        add_round_key(state, get_round_key(self.round_keys, 10))
        inv_shift_rows(state)
        inv_sub_bytes(state)

        for rnd in range(9, 0, -1):
            add_round_key(state, get_round_key(self.round_keys, rnd))
            inv_mix_columns(state)
            inv_shift_rows(state)
            inv_sub_bytes(state)

        add_round_key(state, get_round_key(self.round_keys, 0))
        return bytes(state)

    def encrypt(self, data: bytes) -> bytes:
        data = self.pad(data)
        out = bytearray()
        for i in range(0, len(data), 16):
            out.extend(self.encrypt_block(data[i:i + 16]))
        return bytes(out)

    def decrypt(self, data: bytes) -> bytes:
        if len(data) % 16 != 0:
            raise ValueError("Ciphertext length must be multiple of 16 bytes")
        out = bytearray()
        for i in range(0, len(data), 16):
            out.extend(self.decrypt_block(data[i:i + 16]))
        return self.unpad(bytes(out))

    def encrypt_file(self, in_path: str, out_path: str):
        with open(in_path, "rb") as f:
            plaintext = f.read()
        ciphertext = self.encrypt(plaintext)
        with open(out_path, "wb") as f:
            f.write(ciphertext)

    def decrypt_file(self, in_path: str, out_path: str):
        with open(in_path, "rb") as f:
            ciphertext = f.read()
        plaintext = self.decrypt(ciphertext)
        with open(out_path, "wb") as f:
            f.write(plaintext)
